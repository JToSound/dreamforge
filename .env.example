# DreamForge environment placeholder file.
# Copy to `.env` ONLY if a future optional integration documents a need.
# The deterministic core MUST NOT read environment variables (MASTER_PROMPT.md §4.1).
# This first release requires NO credentials and performs NO network egress.
